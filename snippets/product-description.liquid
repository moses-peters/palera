{% capture description %}
  {%- if product.empty? -%}
    {{ 'home_page.onboarding.product_description' | t }}
  {%- else -%}
    {{ product.description }}
  {%- endif -%}
{% endcapture %}
{%- if is_tab -%}
  {% assign title = 'products.product.description' | t %}
  {%- render 'tab', id: id, title: title, content: description -%}
{%- else -%}
  <div class="pm-product-description">
    <span class="pm-eyebrow">{{ 'products.product.description' | t }}</span>
    <div class="rte">
      {{ description }}
    </div>
  </div>
{%- endif -%}
